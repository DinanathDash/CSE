/*
sprite.setScale(0.5f, 0.5f);
sprite.setPosition(480 - (texture.getSize().x * 0.5f) / 2, 0);

all other required code are in Q5 and Q6

inside while loop
if (sprite.getPosition().y < 540) {
        sprite.move(0, 2);
    }
*/