import Packages.src.Sports;
import Packages.src.Test;
public class Q10 implements Sports {
    public static void main(String[] args) {
        Test student = new Test();
        student.input("Alice", 1, 90, 95);
        student.output();
        int grandTotal = student.mark1 + student.mark2 + score1 + score2;
        System.out.println("Grand Total: " + grandTotal);
    }
}
